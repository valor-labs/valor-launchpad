import { ValorImagePipe } from './valor-image.pipe';

describe('ValorImagePipe', () => {
  it('create an instance', () => {
    // const pipe = new ValorImagePipe();
    // expect(pipe).toBeTruthy();
  });
});
