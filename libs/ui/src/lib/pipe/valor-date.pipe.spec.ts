import { ValorDatePipe } from './valor-date.pipe';

describe('ValorDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ValorDatePipe();
    expect(pipe).toBeTruthy();
  });
});
