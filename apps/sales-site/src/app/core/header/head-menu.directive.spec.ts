import { HeadMenuDirective } from './head-menu.directive';

describe('HeadMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadMenuDirective();
    expect(directive).toBeTruthy();
  });
});
